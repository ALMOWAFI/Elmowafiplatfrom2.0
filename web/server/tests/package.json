{
  "name": "elmowafy-travels-backend-tests",
  "version": "1.0.0",
  "description": "Backend tests for Elmowafy Travels Platform",
  "scripts": {
    "test": "jest --detectOpenHandles --forceExit",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@types/jest": "^29.5.0",
    "@types/supertest": "^6.0.2",
    "jest": "^29.5.0",
    "mongodb-memory-server": "^8.10.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "./setupTests.js"
    ],
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/tests/"
    ]
  }
}
